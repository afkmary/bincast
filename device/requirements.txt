# Bincast device dependencies
#
# On the Pi:        pip3 install -r requirements.txt
# For mock mode:    pip install requests
#
# gpiozero and RPLCD only import when MOCK is false, so mock mode runs on
# Windows and Mac without them.

requests==2.32.3

# Raspberry Pi only -- these will fail to install elsewhere.
gpiozero==2.0.1
RPi.GPIO==0.7.1
RPLCD==1.3.1
smbus2==0.4.3
